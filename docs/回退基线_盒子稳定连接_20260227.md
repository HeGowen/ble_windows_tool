# 回退基线（盒子稳定连接）- 2026-02-27

本文件用于记录一个可回退节点：

- 目标：确认“测试应用用于盒子 BLE 诊断”可稳定工作。
- 结论：在该基线测试中，盒子可持续连接并持续接收数据，出现 BLE 栈断开时可自动重连并继续收包。

参考测试日志（用户提供）：

- `run_20260227_234419/summary.json`
  - `SessionConnected = 4`
  - `SessionDisconnected = 3`
  - `TotalNotifications = 1925`
  - `TotalFrames = 1925`
  - `LastDataAt` 存在且接近结束时刻

- `run_20260227_234419/runtime.log`
  - 多次出现 `connected and notifications enabled`
  - 出现 `session disconnected by BLE stack` 后可继续 `session connect candidates...` 并重新连接
  - 持续出现 `data heartbeat notifications=... frames=...`

说明：

1. 该基线仅覆盖盒子测试链路（扫描、连接、收包、断开后重连）。
2. 该基线用于后续新增雷达测试功能时的回退点。
3. 后续修改必须保证：不破坏本基线盒子诊断能力。
